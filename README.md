
Python OpenCV scripts using Google's Media Pipe library to capture face, hand, body co-ordinates

Also, builds ML models to capture facial expressions, hand gestures, identify yoga poses 
using the landmark points as input
